[project]
name = "pygrader-web"
version = "1.0.0"
description = "Web version of the pygrader"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "coverage>=7.11.0",
    "flake8>=7.3.0",
    "mypy>=1.18.2",
    "pylint>=4.0.2",
]
